
import 'package:flutter/material.dart';
import 'package:ostadflutter/app.dart'; // Import your MyApp widget

// The main function is the entry point of any Flutter application.
// It calls runApp to start the Flutter UI framework.
void main() {
  // runApp takes a Widget and makes it the root of the widget tree.
  // In this case, MyApp is the root, which in turn contains MaterialApp and ModuleMedia.
  runApp( MyApp());
}